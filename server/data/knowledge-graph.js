"use strict";
/* Knowledge graph extension.
   preppilot.html carries the original 52-topic syllabus, and the browser engine
   depends on it, so that file stays the source of truth for everything it
   defines. This module adds topics on top for the server-side interviewer —
   the thin subjects (one Deep Learning node, one Cloud node, one Security node)
   were the places the old engine ran out of questions and started repeating
   itself within three turns.

   Node shape matches the HTML graph exactly:
     id      stable slug, referenced by SKILL_TOPICS and the report
     c       concept name shown to the candidate
     s       subject bucket, used for "weakest subjects" in analytics
     d       depth 1-5; feeds the difficulty ladder
     keys    words in a resume/JD that make this topic relevant
     q       opening questions (picked at random so two runs differ)
     rubric  what a complete answer contains — this is what grading scores against
     probes  deeper follow-ups once the opening answer lands */

const N = (id, c, s, d, keys, q, rubric, probes) => ({ id, c, s, d, keys, q, rubric, probes });

module.exports = [

  /* ---------------- Data structures & algorithms ---------------- */
  N("dsa-twoptr", "Two pointers & sliding window", "DSA", 3,
    ["two pointer", "sliding window", "subarray", "substring"],
    ["When would you reach for a sliding window instead of a nested loop, and what has to be true about the problem?",
     "How do you handle a variable-size sliding window — when do you shrink it?"],
    ["Window works when the answer is a contiguous run and the metric is monotonic as the window grows",
     "Shrink while the constraint is violated, expand otherwise",
     "Turns O(n²) into O(n) with O(1) extra space",
     "Breaks when elements can be negative in a sum problem"],
    ["Why does the sliding window break for 'longest subarray summing to k' with negative numbers?",
     "How would you adapt it to count, rather than find, qualifying windows?"]),

  N("dsa-binsearch", "Binary search & search on answer", "DSA", 3,
    ["binary search", "search", "sorted", "lower bound", "upper bound"],
    ["Beyond finding an element in a sorted array, when else can you binary search?",
     "How do you avoid the classic off-by-one when binary searching for a boundary?"],
    ["Requires a monotonic predicate, not necessarily a sorted array",
     "Binary search on the answer space for optimisation problems",
     "Invariant discipline: which half is provably safe to discard",
     "lo < hi vs lo <= hi and where the answer lands"],
    ["'Minimum capacity to ship packages in D days' — what are you binary searching over?",
     "Why is mid = lo + (hi-lo)/2 preferred over (lo+hi)/2?"]),

  N("dsa-greedy", "Greedy & exchange arguments", "DSA", 4,
    ["greedy", "interval", "scheduling", "activity selection"],
    ["How do you convince yourself a greedy choice is actually optimal, rather than just plausible?",
     "Walk me through interval scheduling — which greedy key do you sort on and why?"],
    ["Exchange argument: swapping to the greedy choice never makes the solution worse",
     "Sort by earliest finish time for max non-overlapping intervals",
     "Greedy fails when a local optimum forecloses a better global one",
     "Contrast with DP: greedy commits, DP explores"],
    ["Give me a scheduling variant where greedy fails and you need DP.",
     "Why does sorting by shortest duration give the wrong answer?"]),

  N("dsa-backtrack", "Backtracking & pruning", "DSA", 4,
    ["backtracking", "permutation", "n-queens", "sudoku", "recursion"],
    ["What separates backtracking from brute-force enumeration?",
     "How do you prune a backtracking search, and how do you know the pruning is safe?"],
    ["Builds partial solutions and abandons a branch the moment it can't extend",
     "Pruning must be provably conservative — never cut a branch containing a solution",
     "State restore on the way back up the recursion",
     "Complexity is branching factor ^ depth, reduced by pruning"],
    ["How would you avoid generating duplicate permutations when the input has repeats?",
     "What's the space complexity, and what dominates it?"]),

  N("dsa-union", "Union-Find / disjoint sets", "DSA", 4,
    ["union find", "disjoint set", "dsu", "kruskal", "connected component"],
    ["Explain union-find and the two optimisations that make it fast.",
     "Where would you reach for union-find over a BFS flood fill?"],
    ["Path compression flattens the tree on find",
     "Union by rank/size keeps trees shallow",
     "Near-constant inverse-Ackermann amortised cost",
     "Good for incremental connectivity; bad for deletions"],
    ["Why can't union-find handle edge deletion efficiently?",
     "How does Kruskal's algorithm use it?"]),

  /* ---------------- Operating systems ---------------- */
  N("os-deadlock", "Deadlock & livelock", "OS", 4,
    ["deadlock", "livelock", "mutex", "lock ordering", "starvation"],
    ["What four conditions have to hold simultaneously for a deadlock, and which one is easiest to break in practice?",
     "You suspect a deadlock in production. How do you confirm it and what do you do?"],
    ["Mutual exclusion, hold-and-wait, no preemption, circular wait",
     "Breaking circular wait via a global lock ordering is the usual practical fix",
     "Detection via thread dumps / lock graphs; prevention beats detection",
     "Livelock: threads keep moving but make no progress"],
    ["How does a lock-ordering convention actually get enforced across a team?",
     "What's the difference between a deadlock and simple lock contention, from the symptoms?"]),

  N("os-vm", "Virtual memory & paging", "OS", 4,
    ["virtual memory", "paging", "page fault", "tlb", "swap", "mmu"],
    ["Walk me through what happens on a page fault, from the instruction to the resumed execution.",
     "What is the TLB and why does it matter so much for performance?"],
    ["Virtual to physical translation via page tables, cached in the TLB",
     "Minor fault (page in memory, not mapped) vs major fault (disk read)",
     "Thrashing when the working set exceeds physical memory",
     "Page replacement policies: LRU approximations, clock"],
    ["Why can a process show a huge virtual size but a small resident size?",
     "How does memory overcommit interact with the OOM killer?"]),

  /* ---------------- Databases ---------------- */
  N("db-isolation", "Isolation levels & anomalies", "DBMS", 4,
    ["isolation", "read committed", "repeatable read", "serializable", "mvcc", "phantom"],
    ["Walk me up the isolation levels and tell me which anomaly each one removes.",
     "What's a phantom read, and which isolation level is the first to prevent it?"],
    ["Read uncommitted → dirty read; read committed → non-repeatable read; repeatable read → phantom; serializable",
     "MVCC gives readers a snapshot without blocking writers",
     "Postgres repeatable read already prevents phantoms; the SQL standard doesn't require it",
     "Higher isolation costs concurrency and raises serialisation failures"],
    ["Your app gets intermittent serialisation failures at SERIALIZABLE. What do you change?",
     "How does MVCC create the need for vacuuming?"]),

  N("db-partition", "Sharding & partitioning", "DBMS", 5,
    ["sharding", "partition", "shard key", "horizontal scaling", "hot partition"],
    ["How would you choose a shard key, and what goes wrong if you choose badly?",
     "What breaks once your data is sharded that used to be free?"],
    ["Even distribution and query locality are both required of a shard key",
     "Hot partitions from low-cardinality or monotonic keys",
     "Cross-shard joins and transactions become expensive or impossible",
     "Resharding is the hard part — consistent hashing limits data movement"],
    ["Why is auto-increment ID a poor shard key for a write-heavy table?",
     "How would you run a migration that changes the shard key with no downtime?"]),

  N("db-locking", "Locking, contention & the N+1 problem", "DBMS", 3,
    ["lock", "row lock", "n+1", "orm", "contention", "select for update"],
    ["What is the N+1 query problem and how do you detect it?",
     "When would you reach for SELECT ... FOR UPDATE, and what does it cost?"],
    ["N+1: one query per row of a parent result set, usually from lazy ORM loading",
     "Fixed by eager loading / a join / a batched IN query",
     "FOR UPDATE takes row locks until commit — keep the transaction short",
     "Long transactions amplify contention and block vacuum"],
    ["How would you find an N+1 in a service you didn't write?",
     "What's the deadlock risk from two transactions locking rows in different orders?"]),

  /* ---------------- Networking ---------------- */
  N("cn-tls", "TLS, certificates & HTTPS", "CN", 4,
    ["tls", "ssl", "https", "certificate", "handshake", "mtls"],
    ["Walk me through what happens in a TLS handshake and what each side proves.",
     "What does a certificate authority actually guarantee?"],
    ["Key exchange establishes a shared secret; the cert authenticates the server",
     "CA chain of trust rooted in the OS/browser trust store",
     "TLS gives confidentiality, integrity and server authentication — not authorisation",
     "mTLS additionally authenticates the client"],
    ["What exactly breaks when a certificate expires, and why doesn't the data become readable?",
     "Why doesn't HTTPS protect you from SQL injection?"]),

  N("cn-dns", "DNS resolution & caching", "CN", 3,
    ["dns", "resolution", "ttl", "cname", "a record", "nameserver"],
    ["Trace what happens between typing a URL and getting an IP address.",
     "Why does a DNS change take time to propagate, and what controls that?"],
    ["Recursive resolver → root → TLD → authoritative nameserver",
     "TTL controls cache lifetime at each layer",
     "Stale caches are why 'propagation' takes time; lowering TTL beforehand helps",
     "A vs CNAME vs ALIAS at the zone apex"],
    ["How would you cut over a domain to a new host with near-zero downtime?",
     "Why can DNS-based load balancing distribute traffic unevenly?"]),

  /* ---------------- System design ---------------- */
  N("sd-consistency", "Consistency, CAP & replication", "SysDesign", 5,
    ["cap theorem", "consistency", "eventual consistency", "replication", "quorum", "partition tolerance"],
    ["Explain the CAP theorem in terms of a concrete decision you'd actually make.",
     "What does eventual consistency mean for the user of your system?"],
    ["Under a network partition you choose availability or consistency; you don't choose P",
     "Quorum reads/writes: R + W > N gives read-your-writes",
     "Eventual consistency surfaces as stale reads — the UX has to tolerate it",
     "Strong consistency costs latency, especially cross-region"],
    ["Your user updates their profile and the next page load shows the old value. Why, and what are the fixes?",
     "When is eventual consistency clearly unacceptable?"]),

  N("sd-ratelimit", "Rate limiting & backpressure", "SysDesign", 4,
    ["rate limit", "throttle", "token bucket", "leaky bucket", "backpressure", "429"],
    ["How would you implement a rate limiter for an API across several servers?",
     "What's the difference between rate limiting and backpressure?"],
    ["Token bucket allows bursts; leaky bucket smooths; fixed window has edge bursts",
     "Distributed limiting needs shared state — Redis counters with expiry",
     "Return 429 with Retry-After rather than dropping silently",
     "Backpressure pushes the limit upstream instead of rejecting at the edge"],
    ["Why does a fixed-window counter allow twice the intended rate at a window boundary?",
     "What happens to your limiter when Redis is briefly unavailable?"]),

  N("sd-idempotency", "Idempotency & retries", "SysDesign", 4,
    ["idempotent", "retry", "exactly once", "duplicate", "idempotency key"],
    ["A payment request times out. The client retries. How do you stop double-charging?",
     "What makes an operation idempotent, and which HTTP methods are supposed to be?"],
    ["Idempotency key stored with the result, deduplicated server-side",
     "GET/PUT/DELETE idempotent by spec; POST is not",
     "'Exactly once' delivery is effectively at-least-once plus idempotent handling",
     "Retries need jitter and a budget, or they amplify an outage"],
    ["Where do you store idempotency keys, and when can you expire them?",
     "Why does retrying without jitter make an overload worse?"]),

  N("sd-observability", "Observability: logs, metrics, traces", "SysDesign", 4,
    ["observability", "monitoring", "logging", "tracing", "metrics", "prometheus", "alert"],
    ["A user says the app is slow. Walk me through how you'd find out why.",
     "What's the difference between metrics, logs and traces, and when does each one help?"],
    ["Metrics for aggregate trends and alerting (RED/USE method)",
     "Traces for a single request across services — where the time went",
     "Logs for the detail once you know where to look",
     "Alert on symptoms users feel, not on causes"],
    ["Why is alerting on CPU usually a bad idea?",
     "What's a correlation/trace ID and how does it flow across service boundaries?"]),

  N("sd-loadbalance", "Load balancing & health checks", "SysDesign", 3,
    ["load balancer", "nginx", "round robin", "health check", "sticky session", "failover"],
    ["What load balancing strategies do you know, and when does round-robin let you down?",
     "What makes a good health check, versus one that lies to you?"],
    ["Round robin, least connections, consistent hashing for cache affinity",
     "Round robin fails with heterogeneous request cost or server capacity",
     "A health check must exercise dependencies the request path actually needs",
     "Shallow checks cause traffic to be sent to broken instances"],
    ["Why can a health check that only returns 200 from the web layer be dangerous?",
     "What breaks when you need sticky sessions, and how do you avoid needing them?"]),

  /* ---------------- Machine learning ---------------- */
  N("ml-leakage", "Data leakage & validation design", "AIML", 5,
    ["leakage", "data leakage", "train test split", "cross validation", "temporal split"],
    ["What is data leakage, and what's the most common way it sneaks into a project?",
     "How would you set up validation for a model predicting something that happens in the future?"],
    ["Leakage = information at training time that won't exist at prediction time",
     "Fitting the scaler/encoder before splitting is the classic mistake",
     "Time-series needs a temporal split, never a random one",
     "Group leakage when the same entity appears in train and test"],
    ["Your offline AUC is 0.95 and production performance is near random. Where do you look first?",
     "Why is a random split wrong for predicting customer churn next month?"]),

  N("ml-imbalance", "Class imbalance", "AIML", 4,
    ["imbalance", "smote", "class weight", "rare event", "fraud", "churn"],
    ["Only 2% of your samples are positive. What changes about how you train and evaluate?",
     "What are the tradeoffs between resampling and class weighting?"],
    ["Accuracy is useless; use precision/recall, F1, PR-AUC",
     "Class weights adjust the loss; oversampling risks overfitting duplicates",
     "SMOTE must be applied inside the CV fold, not before splitting",
     "Threshold tuning is often more effective than resampling"],
    ["Why can SMOTE applied before the split inflate your validation score?",
     "How do you pick the decision threshold, and what business input do you need?"]),

  N("ml-interpret", "Model interpretability", "AIML", 4,
    ["interpretability", "explainability", "shap", "lime", "feature importance"],
    ["A stakeholder asks why the model flagged a specific customer. How do you answer?",
     "What's the difference between global and local interpretability?"],
    ["Global: which features matter overall; local: why this one prediction",
     "SHAP gives additive per-prediction attributions with consistency guarantees",
     "Tree feature_importances_ is biased toward high-cardinality features",
     "Correlation in features makes attributions unstable and easy to misread"],
    ["Why can permutation importance mislead you when two features are correlated?",
     "When is a simpler, less accurate model the right choice?"]),

  N("ml-drift", "Drift & model monitoring", "AIML", 5,
    ["drift", "data drift", "concept drift", "monitoring", "retraining", "production ml"],
    ["How do you know a deployed model has gone stale, if you don't have labels yet?",
     "What's the difference between data drift and concept drift?"],
    ["Data drift: input distribution moves. Concept drift: the input→output relationship moves",
     "Monitor input distributions and prediction distributions when labels lag",
     "PSI / KS tests as tripwires, not as proof",
     "Retraining cadence should be driven by measured decay, not a calendar"],
    ["Labels arrive 60 days late. What do you monitor in the meantime?",
     "What would make you roll back a model rather than retrain it?"]),

  /* ---------------- Deep learning ---------------- */
  N("dl-train", "Training dynamics & optimisation", "DL", 5,
    ["gradient descent", "adam", "learning rate", "batch size", "vanishing gradient", "optimizer"],
    ["Your training loss won't go down. Walk me through how you'd debug that.",
     "What does the learning rate actually control, and how do you pick one?"],
    ["Sanity check: can the model overfit a single batch?",
     "Too-high LR diverges; too-low stalls — LR schedules and warmup",
     "Vanishing/exploding gradients; residual connections and normalisation as fixes",
     "Batch size interacts with LR and with generalisation"],
    ["Why does batch norm behave differently at train and inference time?",
     "What would make you suspect a bug in the data pipeline rather than the model?"]),

  N("dl-transformer", "Attention & Transformers", "DL", 5,
    ["transformer", "attention", "self attention", "positional encoding", "bert", "gpt"],
    ["Explain self-attention — what is being computed and why it helps over an RNN.",
     "Why do Transformers need positional encodings when RNNs don't?"],
    ["Query/key/value; attention weights are learned pairwise relevance",
     "Parallel over sequence length, unlike sequential RNN steps",
     "O(n²) cost in sequence length is the main limitation",
     "Self-attention is permutation-invariant, hence explicit position information"],
    ["What's the practical consequence of the quadratic attention cost, and what do people do about it?",
     "How does an encoder-only model like BERT differ in use from a decoder-only one?"]),

  N("dl-transfer", "Transfer learning & fine-tuning", "DL", 4,
    ["transfer learning", "fine tuning", "pretrained", "freeze", "lora", "peft"],
    ["You have 2,000 labelled images. How do you approach the model?",
     "Which layers do you freeze when fine-tuning, and why?"],
    ["Pretrained features generalise; small data can't learn them from scratch",
     "Freeze early generic layers, fine-tune later task-specific ones",
     "Lower learning rate for pretrained weights than for the new head",
     "LoRA/PEFT adapt large models by training a small number of parameters"],
    ["What's catastrophic forgetting and how does it show up?",
     "When is full fine-tuning worth it over parameter-efficient methods?"]),

  /* ---------------- GenAI / LLM ---------------- */
  N("llm-prompt", "Prompting & context engineering", "GenAI", 4,
    ["prompt", "prompt engineering", "few shot", "system prompt", "context window"],
    ["What actually changes model behaviour in a prompt, in your experience?",
     "How do you decide between few-shot examples and a fine-tune?"],
    ["Clear task framing and output format beat clever phrasing",
     "Few-shot examples teach format and edge cases cheaply",
     "Context window is a budget — relevance beats volume",
     "Fine-tuning pays off for consistent style/format at scale, not for knowledge"],
    ["Why can adding more context sometimes make answers worse?",
     "How would you evaluate a prompt change — what's your test set?"]),

  N("llm-eval", "Evaluating LLM systems", "GenAI", 5,
    ["llm eval", "evaluation", "hallucination", "llm as judge", "benchmark", "regression"],
    ["How do you tell whether a change to your LLM app made it better?",
     "What are the pitfalls of using an LLM to grade another LLM?"],
    ["A fixed eval set with graded examples, run before and after every change",
     "LLM-as-judge is biased toward verbosity and its own style; calibrate against humans",
     "Separate retrieval quality from generation quality",
     "Track regressions, not just averages — per-case diffs"],
    ["How would you build an eval set for a support chatbot from scratch?",
     "Why is a single aggregate score dangerous here?"]),

  N("llm-agents", "Tool use & agent loops", "GenAI", 5,
    ["agent", "tool use", "function calling", "react", "orchestration", "mcp"],
    ["What's the actual control flow of an LLM agent that can call tools?",
     "When does an agent loop become the wrong architecture?"],
    ["Model returns a tool call; your code executes it and returns the result; loop until done",
     "Deterministic workflows beat agents when the steps are known in advance",
     "Failure modes: loops, runaway cost, unrecoverable side effects",
     "Guardrails: step budgets, approval gates for destructive actions"],
    ["How do you stop an agent from looping forever or burning unbounded cost?",
     "Which tool calls would you require a human to approve, and why?"]),

  N("rag-advanced", "Advanced retrieval: hybrid, rerank, chunking", "GenAI", 5,
    ["hybrid search", "rerank", "bm25", "chunking", "vector search", "recall"],
    ["Your RAG system retrieves the wrong documents. Walk me through how you'd diagnose and fix it.",
     "What does hybrid search buy you over pure vector search?"],
    ["Measure retrieval alone: recall@k on a labelled query set",
     "BM25 catches exact terms and rare tokens embeddings blur; hybrid fuses both",
     "A cross-encoder reranker fixes ordering after cheap wide retrieval",
     "Chunking strategy matters more than embedding choice in most systems"],
    ["Why does pure semantic search often miss exact product codes or error numbers?",
     "How would you choose chunk size empirically rather than by rule of thumb?"]),

  /* ---------------- Web ---------------- */
  N("web-perf", "Web performance", "Web", 4,
    ["performance", "core web vitals", "lcp", "bundle size", "lazy load", "caching"],
    ["A page is slow on a mid-range phone but fine on your laptop. How do you approach it?",
     "What dominates the time before a user sees content?"],
    ["Measure first: network waterfall, main-thread work, Core Web Vitals",
     "JS bundle size and parse cost dominate on low-end devices",
     "Code splitting, lazy loading, image format and sizing",
     "Caching headers and CDN for repeat visits"],
    ["Why is time-to-interactive often much worse than first paint?",
     "What's the tradeoff of server-side rendering here?"]),

  N("web-auth", "Web auth: sessions, JWT, OAuth", "Web", 4,
    ["jwt", "oauth", "session", "cookie", "authentication", "refresh token", "sso"],
    ["Compare session cookies and JWTs for authenticating a web app.",
     "What problem does OAuth actually solve?"],
    ["Sessions are revocable server-side; JWTs are stateless but hard to revoke early",
     "Short-lived access token + refresh token as the usual compromise",
     "OAuth is delegated authorisation, not authentication — OIDC adds identity",
     "httpOnly + SameSite cookies mitigate XSS/CSRF token theft"],
    ["A user logs out but their JWT is still valid for 20 minutes. Is that acceptable, and what are the fixes?",
     "Why is storing a JWT in localStorage risky?"]),

  /* ---------------- Cloud & DevOps ---------------- */
  N("cloud-iac", "Infrastructure as code & CI/CD", "Cloud", 4,
    ["terraform", "infrastructure as code", "ci/cd", "pipeline", "github actions", "deployment"],
    ["What does infrastructure as code give you that clicking in a console doesn't?",
     "Walk me through what your CI pipeline should do before code reaches production."],
    ["Reviewable, reproducible, version-controlled infrastructure",
     "Drift detection between declared and actual state",
     "Pipeline: lint → test → build → scan → deploy to staging → promote",
     "Rollback plan is part of the deploy, not an afterthought"],
    ["What's the risk of someone changing infrastructure manually in the console?",
     "How do you keep secrets out of the pipeline and the repo?"]),

  N("cloud-scaling", "Autoscaling & cost", "Cloud", 4,
    ["autoscaling", "cost", "serverless", "cold start", "spot instance", "right sizing"],
    ["Your cloud bill doubled with no traffic change. How do you investigate?",
     "What signal would you autoscale on, and why not CPU?"],
    ["Cost attribution by tag/service before guessing",
     "Autoscale on a signal correlated with user-facing load — queue depth, RPS, latency",
     "CPU lags and misleads for IO-bound services",
     "Serverless cold starts vs always-warm capacity tradeoff"],
    ["What's the failure mode of autoscaling on a lagging metric?",
     "When is serverless clearly the wrong choice?"]),

  N("cloud-containers", "Containers in production", "Cloud", 4,
    ["docker", "kubernetes", "container", "image", "probe", "orchestration"],
    ["What's actually in a container image, and how is it different from a VM?",
     "How does Kubernetes know your container is healthy, and how does it differ from ready?"],
    ["Container shares the host kernel; a VM virtualises hardware and runs its own kernel",
     "Layered images; smaller base images reduce attack surface and pull time",
     "Liveness restarts a hung container; readiness removes it from the load balancer",
     "Resource requests/limits drive scheduling and OOM behaviour"],
    ["What happens if your liveness probe is too aggressive?",
     "Why can a container be running but not ready to serve traffic?"]),

  /* ---------------- Security ---------------- */
  N("sec-injection", "Injection & input handling", "Security", 4,
    ["sql injection", "xss", "csrf", "sanitize", "owasp", "input validation"],
    ["Explain SQL injection and the one fix that actually works.",
     "What's the difference between XSS and CSRF, and what stops each?"],
    ["Parameterised queries separate code from data — escaping is not equivalent",
     "XSS: attacker script runs in your page; fix by context-aware output encoding + CSP",
     "CSRF: attacker makes the victim's browser send an authenticated request; fix with tokens/SameSite",
     "Validate input, but encode on output — they're different defences"],
    ["Why isn't 'escaping quotes' a reliable defence against SQL injection?",
     "Where exactly does the encoding have to happen to stop stored XSS?"]),

  N("sec-secrets", "Secrets & credential handling", "Security", 4,
    ["secret", "api key", "credentials", "vault", "rotation", "environment variable"],
    ["An API key gets committed to a public repo. Walk me through your next thirty minutes.",
     "How should secrets reach a running service?"],
    ["Rotate first — assume it is already scraped; then revoke and audit usage",
     "Rewriting git history does not undo exposure",
     "Secrets from a manager/vault injected at runtime, never baked into images",
     "Least privilege and short TTLs limit blast radius"],
    ["Why is removing the commit not enough?",
     "What would you put in place so this can't recur?"]),

  /* ---------------- Software engineering ---------------- */
  N("se-review", "Code review & collaboration", "SE", 3,
    ["code review", "pull request", "pair programming", "feedback", "git"],
    ["What makes a code review useful rather than a rubber stamp?",
     "You disagree with a reviewer and neither of you is moving. What do you do?"],
    ["Review for correctness, readability and design — not style a linter can catch",
     "Small PRs get real review; large ones get approved unread",
     "Ask questions rather than issue verdicts",
     "Escalate with data or a prototype, and disagree-and-commit when it's not load-bearing"],
    ["How would you review a 2,000-line PR?",
     "What belongs in automation rather than in a human's review?"]),

  N("se-debug", "Debugging methodology", "SE", 3,
    ["debugging", "root cause", "reproduce", "bisect", "postmortem"],
    ["Walk me through how you debug something you've never seen before.",
     "Tell me about the hardest bug you've found and how you found it."],
    ["Reproduce reliably before changing anything",
     "Binary search the problem space — git bisect, disabling halves",
     "Form a hypothesis that makes a falsifiable prediction, then test it",
     "Fix the cause, then add the test that would have caught it"],
    ["What do you do when the bug won't reproduce locally?",
     "How do you avoid the trap of changing several things at once?"]),

  N("se-tech-debt", "Technical debt & refactoring", "SE", 4,
    ["technical debt", "refactor", "legacy", "maintainability", "rewrite"],
    ["How do you decide whether to refactor something or leave it alone?",
     "How would you make the case for paying down tech debt to a manager who wants features?"],
    ["Debt is only worth paying where change is frequent",
     "Refactor behind tests, in small reversible steps",
     "Frame the cost in delivery terms — lead time, incident rate",
     "Rewrites usually underestimate hidden requirements in the old system"],
    ["When is a rewrite actually the right call?",
     "How do you refactor code that has no tests?"]),

  /* ---------------- Data engineering / science ---------------- */
  N("de-modelling", "Data modelling & warehousing", "DataEng", 4,
    ["star schema", "data warehouse", "dimension", "fact table", "etl", "elt", "dbt"],
    ["Explain a star schema and why analytics uses it instead of a normalised model.",
     "What's the difference between ETL and ELT, and what changed to make ELT viable?"],
    ["Fact tables hold measures; dimensions hold descriptive attributes",
     "Denormalised for read performance and query simplicity",
     "ELT loads raw then transforms in the warehouse — cheap compute made this practical",
     "Slowly changing dimensions preserve history"],
    ["How would you handle a dimension attribute that changes over time?",
     "Why is normalisation the wrong default for an analytics table?"]),

  N("ds-experiment", "A/B testing & experiment design", "DataSci", 5,
    ["a/b test", "experiment", "hypothesis test", "p value", "significance", "sample size"],
    ["Design an A/B test for a checkout button change. What do you decide up front?",
     "Your test shows a 3% lift with p = 0.06. What do you tell the product manager?"],
    ["Define metric, MDE, sample size and duration before starting",
     "p = 0.06 is not 'nearly significant' — report the confidence interval",
     "Peeking inflates false positives; fix the horizon or use sequential testing",
     "Novelty effects and weekly seasonality need full-cycle runs"],
    ["What's the multiple comparisons problem and when does it bite you here?",
     "How would you detect a sample ratio mismatch and what would it mean?"]),

  /* ---------------- Testing / QA ---------------- */
  N("qa-strategy", "Test strategy & the pyramid", "QA", 3,
    ["test pyramid", "unit test", "integration test", "e2e", "mock", "coverage", "tdd"],
    ["What's your split between unit, integration and end-to-end tests, and why?",
     "How do you test code that depends on an external API?"],
    ["Many fast unit tests, fewer integration, fewest E2E — speed and reliability",
     "Mocks/stubs/fakes differ; fakes tend to be the most maintainable",
     "Contract tests catch schema drift at service boundaries",
     "Coverage is a smoke detector, not a goal"],
    ["Why is 100% coverage not a useful target?",
     "What makes an E2E suite flaky and what do you do about it?"]),

  /* ---------------- Mobile ---------------- */
  N("mob-state", "Mobile state, lifecycle & offline", "Mobile", 4,
    ["android", "ios", "lifecycle", "offline", "sync", "react native", "flutter"],
    ["How do you handle an app that needs to work offline and sync later?",
     "What does the activity/view lifecycle force you to think about?"],
    ["Local store as source of truth, queue mutations, reconcile on reconnect",
     "Conflict resolution policy must be explicit — last-write-wins loses data",
     "Lifecycle callbacks mean state can be destroyed and recreated at any time",
     "Battery and network constraints shape sync cadence"],
    ["Two devices edit the same record offline. What happens on sync?",
     "Why can't you keep important state only in memory?"]),
];
